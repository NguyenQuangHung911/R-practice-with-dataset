---
title: "Report"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

Khai báo các thư viện sử dụng

```{r}
library(dplyr)
library(openintro)
```

Lấy dữ liệu từ file csv

```{r}
get_it_dunn_run <- read.csv("get_it_dunn_run.csv")
```

2. Thông tin về tập dữ liệu như sau:

| Biến                          |  Mô tả
|:----------------|:--------------------------------
|`date`                       | Ngày tháng
|`race`                       | Loại đường đua
|`bib_num`                    | Năm sinh
|`first_name                 `| Tên đầu
|`last_initial`               | 
|`sex`                        | Giới tính
|`age`                        | Số sinh viên nam
|`city`                       | Số sinh viên nữ
|`state`                      | Tỷ lệ nữ
|`run_time_minutes `          | Số sinh viên có việc làm

```{r}
glimpse(get_it_dunn_run)
```
Các biến phân loại gồm:


Vẽ sơ đồ cho các biến phân loại:

```{r}

```

Các biến số gồm:


Vẽ sơ đồ cho các biến số và tính các summary statistics:

```{r}

```

3. Chọn 3 biến phân loại và 1 biến số:


```{r}

```

4. Kiểm định giả thuyết và xây dựng khoảng tin cậy

```{r}

```

